Starting phptelemeter 0.26, the config parameter "show_graph" was added.

Please put in your config file under the [general] section:
show_graph=true (or false)

OLD NEWS:
~~~~~~~~~
Starting phptelemeter 0.24, the config parameter style has been replaced by the new
publisher parameter. You can set it to either plaintext, html or machine:

publisher=plaintext
or
publiser=machine
or
publisher=html

Starting phptelemeter 0.21, an optional parameter was added to the config file:
modulepath=/path/to/phptelemeter

Starting phptelemeter 0.20, a new configuration parameter was added to the configuration file:
parser=telemeter4tools
OR
parser=telemeter_web

Starting phptelemeter 0.13, two new options are added to the configuration file:

file_output=false
file_prefix=/tmp/phptelemeter_

Starting phptelemeter 0.10, the configuration file format was changed to allow for multiple users.
